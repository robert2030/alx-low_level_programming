#include <stdio.h>

/**
 * main - Entry point
 * Return: Alway return 0 (Success)
 */

int main(void)
{
	char c;

	c = 'a';

	while
		(c <= 'a')
		{
			putchar(c);
				c++;
		}
	putchar('\n');
	return (0);
}
