#include <stdio.h>

/**
 * main - Entry point
 * Return: Alway return 0 (Success)
 */

int main(void)
{
	char b;

	b = 'a';

	while
		(b <= 'a')
		{
			putchar(b);
				b++;
		}
	putchar('\n');
	return (0);
}
